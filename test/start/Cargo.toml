[package]
name = "rtx"
version = "0.0.1"
author = "mills"

[dependencies]

[package.metadata.bootimage]
build-command = ["build"]

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"

run-command = [
    "qemu-system-riscv64", "", "" 
]

